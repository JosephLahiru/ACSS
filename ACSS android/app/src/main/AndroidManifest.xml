<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.acss">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ACSS">
        <activity android:name=".pc_repair"></activity>
        <activity android:name=".warranty_claim" />
        <activity android:name=".select_assembler" />
        <activity android:name=".payment_portal" />
        <activity android:name=".computerAssemblyAct" />
        <activity android:name=".pcRepairWarrantyAct" />
        <activity android:name=".productListAct" />
        <activity android:name=".preBuildAct" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>